"""Source-domain training utilities.

The legacy SHOT ResNet/VGG trainer remains in :mod:`image_source`.  The DeiT
trainer is implemented in separate modules so its checkpoint and model schema
cannot silently alter the legacy three-file format.
"""

